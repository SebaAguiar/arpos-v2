{
  "name": "arcon-launcher",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tauri dev",
        "cwd": "apps/arcon-launcher"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tauri build",
        "cwd": "apps/arcon-launcher"
      }
    }
  },
  "tags": ["scope:launcher", "type:app"]
}

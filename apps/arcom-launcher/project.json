{
  "name": "arcom-launcher",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "targets": {
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tauri dev",
        "cwd": "apps/arcom-launcher"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tauri build",
        "cwd": "apps/arcom-launcher"
      }
    }
  },
  "tags": ["scope:launcher", "type:app"]
}
